pre-commit:
  parallel: true
  commands:
    eslint:
      glob: '*.{js,ts,jsx,tsx}'
      run: npm run lint
    stylelint:
      glob: '*.{css,tsx}'
      run: npm run lint:css
    prettier:
      run: npx prettier --write {staged_files}
